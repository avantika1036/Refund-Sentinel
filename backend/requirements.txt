# =============================================================================
# Refund Sentinel — Backend Dependencies
# Python 3.11+
#
# Packages are pinned to minor versions (X.Y.*) to allow patch updates
# while preventing unexpected breaking changes.
# Run: pip install -r backend/requirements.txt
# =============================================================================


# =============================================================================
# Web framework
# =============================================================================
fastapi==0.115.*
uvicorn[standard]==0.30.*


# =============================================================================
# Data validation and settings
# pydantic v2 is required. pydantic-settings reads from .env files.
# =============================================================================
pydantic==2.7.*
pydantic-settings==2.3.*


# =============================================================================
# Database
# SQLAlchemy 2.x with async-compatible psycopg (v3) driver.
# psycopg3 (package name: psycopg) is the modern PostgreSQL driver.
# psycopg[binary] bundles the libpq binary; use psycopg[c] in production
# for better performance if libpq is available on the host.
# =============================================================================
sqlalchemy==2.0.*
psycopg[binary]==3.1.*
alembic==1.13.*


# =============================================================================
# Testing
# pytest-asyncio is needed for testing async FastAPI endpoints.
# httpx is required by FastAPI's TestClient for async support.
# =============================================================================
pytest==8.2.*
pytest-asyncio==0.23.*
httpx==0.27.*


# =============================================================================
# Development utilities
# python-dotenv allows loading .env files during local development
# outside of pydantic-settings (e.g., scripts, CLI tools).
# =============================================================================
python-dotenv==1.0.*